  
  export default function searchABook(url) {
    return new Promise((resolve, reject) => {
      const BookInfoArray = response.data.items;
      process.nextTick(() =>
        BookInfoArray[index].volumesInfo.title;
        BookInfoArray[index].volumeInfo.authors;
        BookInfoArray[index].volumeInfo.publisher;
          ? resolve(users[index])
          : reject({
              error: 'User with ' + userID + ' not found.',
            }),
      );
    });
  }